/**
 * Some debug programs. However, exporting classes from the libs through OSGi does not work, otherwise
 * it would be better to move these classes into the related test bundle.
 */
package eu.qualimaster.easy.extension.debug;